{
  "name": "example",
  "scripts": {
    "postinstall": "elm-tooling install",
    "start": "run-pty % npm run start:elm % npm run start:http",
    "start:elm": "elm-watch hot",
    "start:http": "serve public -s"
  },
  "devDependencies": {
    "elm-tooling": "^1.15.1",
    "elm-watch": "^1.2.2",
    "run-pty": "^5.0.0",
    "serve": "^14.2.4"
  }
}
